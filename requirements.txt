# Prestige Group — Pipeline Dependencies
# Install with:  pip install -r requirements.txt

# maps_scraper.py
requests>=2.31.0
urllib3>=2.0

# website_cloner.py (also uses requests above)
# No additional deps — uses wget/curl if available, falls back to requests